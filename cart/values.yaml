resources:
  limits:
    cpu: 200m
    memory: 200Mi
  requests:
    cpu: 100m
    memory: 100Mi
restartPolicy: Always
image:
  repo: pullapantangi
  tag: latest
  name: ecomm_cart
labels:
  app_type: cart
imagePullPolicy: Always
service:
  ports:
  - name: cart
    port: 8080
    targetPort: 8080
