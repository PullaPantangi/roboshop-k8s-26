apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: cls-fit
  namespace: argocd
spec:
  generators:
    - list:
        elements:
          - env: fit
            product: cls
            path: mongodb
          - env: fit
            prdocut: cls
            path: redis
          - env: fit
            prdocut: cls
            path: user
  template:
    metadata:
      name: '{{env}}-{{product}}-{{path}}'
      namespace: argocd
    spec:
      project: cls-fit
      source:
        repoURL: https://github.com/PullaPantangi/roboshop-k8s-26.git
        targetRevision: master
        path: 'applications/{{path}}'
      destination:
        server: https://kubernetes.default.svc
        namespace: '{{path}}'
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true

