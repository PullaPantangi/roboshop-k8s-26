resources:
  limits:
    cpu: 200m
    memory: 200Mi
  requests:
    cpu: 100m
    memory: 100Mi
restartPolicy: Always
image:
  repo: pullapantangi
  tag: latest
  name: robo_rmq
labels:
  app_type: rabbitmq
imagePullPolicy: Always
service:
  ports:
  - name: mysql
    port: 8080
    targetPort: 8080
