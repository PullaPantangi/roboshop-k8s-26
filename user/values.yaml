resources:
  limits:
    cpu: 200m
    memory: 200Mi
  requests:
    cpu: 100m
    memory: 100Mi
restartPolicy: Always
image:
  repo: pullappantangi
  tag: latest
  name: robo_user
labels:
  app_type: user
imagePullPolicy: always
service:
  ports:
  - name: user
    port: 8080
    targetPort: 8080
